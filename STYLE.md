# 執筆ガイド(全巻共通)

> 原稿を書くすべての執筆者(人間・エージェント問わず)は、執筆前に CLAUDE.md → DESIGN.md → 担当巻の TOC.md → 本ファイルの順に読むこと。

## 文体

- **です・ます調**。講義の語り口。一人称は「私たち」、読者への呼びかけは「あなた」(多用しない)
- 1つの段落で1つのこと。長い段落より短い段落を重ねる
- 問いを先に立ててから答える(「なぜ〜でしょうか」→ 説明)。需要駆動の原則を文章レベルでも守る: **新しい道具は、困ってから出す**
- 比喩は1概念につき1つまで。比喩を重ねない
- 用語の初出は英語を併記する: 「内積(dot product)」。2回目以降は日本語のみ
- 「簡単です」「自明です」は禁止。読者が詰まる場所を著者が先回りして言語化する

## 章の構成テンプレート

1. **導入**(この章の需要の確認): 前章までで何ができて、何に困っているか。1〜3段落
2. **本文**: 定義は「直観 → 形式化」の順。いきなり定義から始めない
3. **コード**(TOC で [コード] 指定がある節のみ)
4. **まとめ**: 箇条書き3〜5点 + 「ラスボスとの距離」(論文のどの記号に近づいたか)1行
5. **演習**: 2〜4問。各問に「略解」を付す(別ファイルにしない、章末に畳んで置く)

分量目安: 1章 8,000〜15,000字(コード含む)。序章・終章は 4,000〜8,000字。

## 数式・記法

- 数式は LaTeX(`$...$` / `$$...$$`)
- 行列は大文字 $X, W$、ベクトルは小文字太字 $\mathbf{x}$、スカラーは小文字 $a$
- **shape 注釈を必ず書く**(第1巻3.2で導入するシリーズ共通の規律): 行列・ベクトルの初出時に `(行数, 列数)` を併記。例: $X$ `(n, d)`
- 論文の記号(Q, K, V, d_k, d_model, W^Q …)は論文の表記に厳密に合わせる
- 証明は原則しない。直観・小さな数値例・コードでの確認で代える(例外は TOC に明記された導出のみ: 4巻5.5など)

## ラスボス引用の書式

論文の原文引用は引用ブロックで、英語原文 → 和訳の順:

> *"Attention(Q, K, V) = softmax(QK^T / √d_k) V"*
> — Vaswani et al., "Attention Is All You Need", Section 3.2.1
>
> 訳: (和訳をここに)

巻頭(序章)と巻末(終章)で**同一の引用**を使うこと。終章では「巻頭では読めなかったが、いま読める」ことを明示的に確認する。

## コード規約

- Python 3.9 互換(型ヒントは `list[int]` 等の新記法を避ける)。NumPy(`import numpy as np`)。第8巻2章以降のみ PyTorch 解禁
- 乱数は必ず `rng = np.random.default_rng(42)` で固定
- 変数名はシリーズ共通: データ `X`, `y`、パラメータ `W`, `b`、学習率 `lr`、損失 `loss`、勾配 `grad_W` 形式
- 動作確認は `assert`(数値比較は `np.allclose`)。「実行すると一致が確認できる」形で完結させる
- 本文中のコードは**全文掲載・全文実行可能**にする(断片の掲載は「…の中身はこうです」という再掲のみ可)
- 主要実装は `code/chNN/` に .py としても保存し、`python3 ファイル名.py` で assert が通ること
- matplotlib は**コード掲載のみ**(執筆環境に未導入のため実行検証外)。図は `図N.M:` のキャプション付きで、何が見えるはずかを本文で必ず言語化する
- コードのコメントは「コードだけでは読めない制約」のみ。逐行解説は本文の役割

## 伏線の規律(シリーズの生命線)

- TOC に書かれた伏線・回収は**省略禁止**。張るときは「第N巻で回収します」と明示し、回収するときは「第N巻N章で予告した〜です」と明示する
- TOC にない新しい伏線を勝手に張らない(回収保証がないため)
- 巻をまたぐ参照は「第N巻N章」形式。同一巻内は「第N章」

## 各巻の担当ファイル

- 原稿: `volN-*/manuscript/NN-slug.md`(NN は 00=序章 から連番、終章が最大番号)
- コード: `volN-*/code/chNN/`(章と対応。[コード] 指定のない章はディレクトリ不要)
- TOC.md・paper-map.md は執筆中に変更しない(矛盾を見つけたら原稿側に TODO コメント `<!-- TODO(toc): ... -->` を残す)

## 文体サンプル(この調子で書く)

> 前の章で、ベクトル同士の「似ている度合い」を内積で測れるようになりました。ところで、単語が1000個あったらどうしましょうか。1000個のベクトルすべての組み合わせについて、内積を1つずつ計算する——書けなくはありませんが、forループが2重になり、コードも遅くなりそうです。
>
> 実は、この「内積の総当たり」をたった1つの演算で書く方法があります。それが行列積です。
